# yamllint disable rule:line-length
---
name: Build Docker Images

on:  # yamllint disable-line rule:truthy
  push:
    branches:
      - main
      - master
      - dev
      - "defnotean/**"
      - "ianga/**"
  pull_request:
    branches:
      - main
      - master
      - dev
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    outputs:
      date: ${{ steps.date.outputs.date }}
      tag: ${{ steps.branch.outputs.tag }}
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up QEMU
        id: qemu
        uses: docker/setup-qemu-action@v4
        with:
          image: tonistiigi/binfmt:latest
          platforms: all

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev')
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT || github.token }}

      #      - name: Login to Docker Hub
      #        if: github.event_name != 'pull_request'
      #        uses: docker/login-action@v1
      #        with:
      #          username: ${{ secrets.DOCKER_USERNAME }}
      #          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Get current date
        id: date
        run: echo "date=$(date +"%Y-%m-%dT%H:%M:%SZ")" >> "$GITHUB_OUTPUT"

      - name: Get Crafty version
        id: version
        run: |
          MAJOR=$(cat app/config/version.json | jq '.major' )
          MINOR=$(cat app/config/version.json | jq '.minor' )
          SUB=$(cat app/config/version.json | jq '.sub' )
          echo "version=$MAJOR.$MINOR.$SUB" >> "$GITHUB_OUTPUT"

      - name: Get branch tag
        id: branch
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" || "${{ github.ref }}" == "refs/heads/master" ]]; then
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          else
            tag=$(echo "${{ github.ref }}" | sed -e 's/refs\/heads\///g' -e 's/[^A-Za-z0-9_.-]/-/g')
            echo "tag=$tag" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev') && 'linux/arm64,linux/amd64' || 'linux/amd64' }}
          push: ${{ github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev') }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}:${{ steps.branch.outputs.tag }}
          cache-to: type=inline
          build-args: |
            BUILD_DATE=${{ steps.date.outputs.date }}
            BUILD_REF=${{ github.sha }}
            CRAFTY_VER=${{ steps.version.outputs.version }}
          tags: |
            ghcr.io/${{ github.repository }}:${{ steps.branch.outputs.tag }}

#           {{ GITHUB_REPOSITORY }}:${{ steps.branch.outputs.tag }}
